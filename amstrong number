# include<stdio.h>
void main()
{
  int n,ams,r,p=0;
  printf("Enter number\n");
  scanf("%d",&n);
  ams=n;
  while(n>0)
  {
    r=n%10;
    p=p+(r*r*r);
    n=n/10;
  }
  if(p==ams)
  printf("Entered number is amstrong");
  else
  printf("Entered number is not amstrong");
}  
